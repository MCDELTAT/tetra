A simple File Upload Example

Uses Express+Multer for uploads and Jade for templating.

To run it, simply go to the command line and type:
"npm install", this will download all the app dependencies
"node server.js" this run the app

Point your browser to localhost:3000

After you hit the submit button on the file, you should see responses on the request.
